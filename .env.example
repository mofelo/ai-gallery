# AI Gallery

# Astro frontend API base URL (local dev)
# API_BASE=http://localhost:8787

# Worker API base URL (issue-cli 写入/读取图片记录，数据存 D1)
# 本地开发用 http://localhost:8787，生产用 Cloudflare Worker 地址
API_BASE=https://ai-gallery-api.baobaolong12.workers.dev

# issue-cli (required)
# ImgBed 上传地址（纯图床，不存元数据）
IMGBED_URL=https://your-imgbed.example.com

# 注意: 已不再需要 GITHUB_TOKEN —— issue-cli 现在通过 Worker API 写入 D1，
# 不再直接创建 GitHub Issue。