[package]
name = "ai-gallery-core"
version = "0.1.0"
edition = "2021"
description = "AI Gallery core library — data types, error handling, response envelope"

[lib]
crate-type = ["lib", "cdylib"]

[features]
wasm = []

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"